spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.format_sql: true
      hibernate.dalect: org.hibernate.dialect.MySQL55Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/transaction
    username: root
    password: ephesus
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: transaction-service
  main:
    allow-bean-definition-overriding: true
server:
  port: 8097
